name: Auto Update Blog

on:
  schedule:
    - cron: '*/30 * * * *'  # 30分ごと
  workflow_dispatch:  # 手動実行も可能

jobs:
  update-blog:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install httpx python-dotenv aiofiles
    
    - name: Generate new article
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      run: |
        python generate_article.py || echo "Article generation completed"
    
    - name: Commit and push if changed
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git diff --staged --quiet || git commit -m "Auto-update: New articles $(date +'%Y-%m-%d %H:%M')"
        git push